:orphan:

.. Auto-generated by help-rst from "mirtk evaluate-atlas -h" output

evaluate-atlas
==============

.. program:: evaluate-atlas


Synopsis
--------

::

    evaluate-atlas [-h] [-a MEANS [MEANS ...]] [-w TMPDIR]
    [-i STEP [STEP ...]] [-q QUEUE] [-t THREADS]
    [-v VERBOSE]
    config


Description
-----------

.. include:: _descriptions/evaluate-atlas.rst



Arguments
---------

.. option:: config

   JSON file with atlas configuration.


Command options
---------------

.. option:: -h, --help

   show this help message and exit

.. option:: -a MEANS [MEANS ...], --ages MEANS [MEANS ...], --means MEANS [MEANS ...]

   Discrete time points for which to perform evaluation.

.. option:: -w TMPDIR, --workdir TMPDIR, --tmpdir TMPDIR

   Working directory of intermediate files.

.. option:: -i STEP [STEP ...], --step STEP [STEP ...]

   Atlas construction steps to evaluate. Evaluate all
   'iterations' specified in config otherwise (default:
   10).

.. option:: -q QUEUE, --queue QUEUE

   Name of batch system queue. Use 'condor' for HTCondor.
   Otherwise, the argument is assumed to be the name of a
   SLURM partition.

.. option:: -t THREADS, --threads THREADS

   Maximum number of CPU cores/threads to use.

.. option:: -v VERBOSE, --verbose VERBOSE

   Verbosity level of output messages: 0) no output, 1)
   report progress, 2) print command arguments.
